// Import from official npm package (includes full model support)
import * as webllm from './webllm-npm.js';

export const CreateMLCEngine = webllm.CreateMLCEngine;
export const prebuiltAppConfig = webllm.prebuiltAppConfig;
export const MLCEngine = webllm.MLCEngine;
export default webllm;
